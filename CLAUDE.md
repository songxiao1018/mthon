# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目性质

本仓库是 **Multi-TRIX THON 教学手册** 的 Markdown 源文档项目，**非代码项目**。无 `package.json` / `pyproject.toml` / 测试框架，Python 片段作为教学内容内嵌于 Markdown 而非可执行代码。

- **受众**：10–16 岁零基础青少年，对齐 **CCF GESP Python 1–4 级考纲**
- **世界观**：以 Minetest 沙盒游戏为沉浸背景，主角 Multi-Boy / Multi-Girl 串联知识点
- **双轨教学**：纯逻辑编程（`print` / 变量 / 输入）+ Turtle 可视化
- **License**：MIT（仅供学习与教学使用，禁止商用，见 `LICENSE`）

## 顶层结构与内容流向

四个根级 Markdown 是**模板/工作流文档**，不是课程内容：

| 文件 | 角色 | 何时使用 |
| :--- | :--- | :--- |
| `BOOK/000 章节安排.md` | 全书进度总表（15 章 × 4 课型 = 51 课） | 排课、对齐完成度 |
| `BOOK/011 知识点草稿模板.md` | 按**知识点粒度**备课（5 步：情境引入 → 核心知识点 → 代码演示 → 互动提问 → 随堂笔记） | 准备新课时的**第一步** |
| `BOOK/012 教材草稿.md` | 按**课次**统一组装 1.1–1.4 四模块 | 把 011 产物合并为 2 小时完整课 |
| `BOOK/013 考级练习课.md` | GESP 真题刷题课模板 | 13 节考级课 |
| `BOOK/014 章节复习课.md` | 章节综合大项目模板 | 穿插于各章末尾 |

**内容流向（不可逆）**：
```
011 知识点草稿（按知识点拆开写，1 节课 3~5 个单元）
   ↓
012 教材草稿（按 1.1 引入 / 1.2 Bug猎人 / 1.3 创造 / 1.4 复盘 整合）
   ↓
《第 X 课：标题（知识点）》正式课件，存入 BOOK/NN 章节名/ 目录
```

## 课型体系（4 类，全书 51 课）

| 课型 | 图标 | 数量 | 职责 |
| :--- | :--- | :--- | :--- |
| 知识课 | 📘 | 21 | 讲清语法、逻辑、原理 |
| 海龟课 | 🐢 | 17 | 用 Turtle 把代码"画出来" |
| 考级课 | 📝 | 13 | 刷 GESP 真题、避坑训练 |
| 复习课 | 🔄 | 穿插 | 综合项目实战 |

进度以 `BOOK/000 章节安排.md` 中 `<del>` 标记为准。

## 正式课件文件结构

存放在 `BOOK/NN 章节名/` 目录下，命名严格遵循：

- **已完成课**：`NNN 第 X 课：标题（知识点）.md`（如 `001 第 1 课：向世界问好（输出与变量）.md`）
- **进行中草稿**：`草稿 NNN 第 X 课_标题_...md`（**已被 `.gitignore` 的 `草稿*` 规则排除**，不会进入版本控制）
- 章节目录用空格分隔的两位数（`01`、`02`、`11`...），与 `000 章节安排.md` 的章节序号一致

## 写作硬性约定（来自 011 模板的"草稿自检清单"）

每写一节新课前必读 `BOOK/011 知识点草稿模板.md` 末尾的 ✅ 自检清单。以下是**不可违反**的项目：

1. **比喻优先**：每个知识点必须配一个**具体物品/角色**的比喻（`print()`=喊话器，变量=背包，注释=便利贴，`import`=打开工具包，`Turtle()`=召唤助手）。**禁止**用"工具"、"概念"这种空泛词。
2. **情境引入钩子**：必须以"Multi-Boy 冒险者"或"玩家"为主语（不要用"我们"这种含糊视角），1–3 句话、≤ 60 字，最后一句指向本知识点核心动作。
3. **代码可直接运行**：禁止伪代码、`<xxx>` 占位符；每个示例必须带 `#` 行内注释。
4. **第一次运行覆盖全部知识点**：示例之间递进（最小化 → 贴近情境 → 联合使用），帮助学生建立知识点之间的联系。
5. **互动提问必须有"参考解答"**：用 `>` 引用块折叠。
6. **随堂笔记 80–150 字**：5 分钟内可抄完，优先用口诀、顺口溜、表格、问答。
7. **情境/代码/提问/笔记四方呼应同一个 Minetest 主题**，不要一会儿村庄一会儿地下城。
8. **Bug猎人模块**：报错信息尽可能复用"第一次运行"中会出现的错误，便于学生复习。
9. **章节引入的"Multi-TRIX百科"**：严格遵循 `- **知识点 = 比喻**：定义。\n  > 使用方法：…\n  > 示例：…` 三段式格式。

## 知识点的全局编号

`知识点.md` 是全书的**知识点编号总表**（`### 知识点 NNNN 名称`）。新知识点写入课件时，编号须对齐此表，且**顺序按引入顺序追加**（不要跳号、不要复用）。当前已用到 0011（数据类型）。

## 仓库规则（来自 `.gitignore`）

- `.claude/`、`.claude/skills/`、`worktrees/` — Claude 内部状态，**不进版本控制**
- `草稿*` — 所有以"草稿"开头的备课草稿、课件草稿**不进版本控制**（仅正式课件入库）
- 标准 Python `.gitignore` 模板（保留以防后续引入 Python 工具链）

## 课件配套物料（已存在，非 Claude 产出）

- `Multi-TRIX THON 教学手册.xmind` / `.png` — 全书思维导图（XMind 源文件 + 导出图）
- `BOOK/草稿 课件/NoteQuest/NOTEQUEST_EW_cn.pdf` — Minetest NoteQuest Mod 中文文档（用于海龟课情境素材）
- `BOOK/草稿 课件/*.html` / `*.docx` — 旧版互动课件大纲（HTML 可浏览器直开，docx 为 Office 源文件）
